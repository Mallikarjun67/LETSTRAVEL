# must be unique in a given SonarQube instance
sonar.projectKey='TRAVEL1: 5aa2e0f533721edec64a74bb2b859c8ed213a7c7'
sonar.projectName='LETS TRAVEL-main'
sonar.projectVersion='9.18.0'
sonar.exclusions=**/*.java
#sonar.excludePlugins=web
#sonar.exclusions=themesource/atlas_core/native/core/helpers/linechart.js

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.

#----- Default SonarQube server
#sonar.host.url=http://localhost:9000
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# must be unique in a given SonarQube instance

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.

#----- Default SonarQube server
#sonar.host.url=http://localhost:9000
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8